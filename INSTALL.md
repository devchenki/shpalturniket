# Руководство по установке - Shaplych Monitoring System

## 📋 Содержание

- [Системные требования](#системные-требования)
- [Быстрая установка](#быстрая-установка)
- [Ручная установка](#ручная-установка)
- [Docker установка](#docker-установка)
- [Настройка](#настройка)
- [Проверка установки](#проверка-установки)
- [Устранение проблем](#устранение-проблем)

## 💻 Системные требования

### Минимальные требования
- **ОС**: Windows 10/11, Ubuntu 20.04+, macOS 10.15+
- **RAM**: 4GB (рекомендуется 8GB)
- **Диск**: 2GB свободного места
- **Сеть**: Доступ к интернету

### Программное обеспечение
- **Python**: 3.11+ (обязательно)
- **Node.js**: 18+ (обязательно)
- **Git**: 2.30+ (рекомендуется)
- **Docker**: 20.10+ (опционально)

### Сетевые требования
- **Порты**: 8771 (backend), 5181 (frontend)
- **Firewall**: Разрешить входящие соединения на порты 8771, 5181
- **Интернет**: Для Telegram API, загрузки зависимостей

## 🚀 Быстрая установка

### Windows

1. **Скачайте проект**
```cmd
git clone https://github.com/your-repo/shaplych-monitoring.git
cd shaplych-monitoring
```

2. **Запустите автоматическую установку**
```cmd
start.bat
```

3. **Откройте браузер**
```
http://localhost:5181
```

### Linux/macOS

1. **Скачайте проект**
```bash
git clone https://github.com/your-repo/shaplych-monitoring.git
cd shaplych-monitoring
```

2. **Запустите автоматическую установку**
```bash
chmod +x install.sh
./install.sh
```

3. **Откройте браузер**
```
http://localhost:5181
```

## 🔧 Ручная установка

### Шаг 1: Установка Python

#### Windows
1. Скачайте Python 3.11+ с [python.org](https://www.python.org/downloads/)
2. Установите с опцией "Add Python to PATH"
3. Проверьте установку:
```cmd
python --version
pip --version
```

#### Linux (Ubuntu/Debian)
```bash
sudo apt update
sudo apt install python3.11 python3.11-pip python3.11-venv
```

#### macOS
```bash
# Через Homebrew
brew install python@3.11

# Или скачайте с python.org
```

### Шаг 2: Установка Node.js

#### Windows
1. Скачайте Node.js 18+ с [nodejs.org](https://nodejs.org/)
2. Установите LTS версию
3. Проверьте установку:
```cmd
node --version
npm --version
```

#### Linux (Ubuntu/Debian)
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

#### macOS
```bash
# Через Homebrew
brew install node@18

# Или скачайте с nodejs.org
```

### Шаг 3: Установка зависимостей

#### Backend
```bash
# Создание виртуального окружения
python -m venv venv

# Активация (Windows)
venv\Scripts\activate

# Активация (Linux/macOS)
source venv/bin/activate

# Установка зависимостей
cd backend
pip install -r requirements.txt
```

#### Frontend
```bash
cd frontend
npm install
```

### Шаг 4: Настройка конфигурации

1. **Скопируйте пример конфигурации**
```bash
cp env.example .env
```

2. **Отредактируйте .env файл**
```bash
# Основные настройки
DATABASE_URL=sqlite:///./shaplych_monitoring.db
SECRET_KEY=your-secret-key-here
DEBUG=true

# Telegram Bot
TELEGRAM_TOKEN=your-bot-token-here
TELEGRAM_CHAT_IDS=123456789,987654321

# API
VITE_API_URL=http://127.0.0.1:8771/api
```

3. **Настройте устройства**
```bash
# Отредактируйте IP_list.json
{
  "T001": ["192.168.1.100", "Турникет вход", "1"],
  "T002": ["192.168.1.101", "Турникет выход", "1"]
}
```

### Шаг 5: Запуск приложения

#### Терминал 1 - Backend
```bash
cd backend
python -m uvicorn app.main:app --host 127.0.0.1 --port 8771 --reload
```

#### Терминал 2 - Frontend
```bash
cd frontend
npm run dev -- --port 5181
```

#### Терминал 3 - Telegram Bot (опционально)
```bash
python advanced_bot.py
```

## 🐳 Docker установка

### Предварительные требования
- Docker 20.10+
- Docker Compose 2.0+

### Установка

1. **Клонирование репозитория**
```bash
git clone https://github.com/your-repo/shaplych-monitoring.git
cd shaplych-monitoring
```

2. **Настройка переменных окружения**
```bash
cp env.example .env
# Отредактируйте .env файл
```

3. **Сборка и запуск**
```bash
docker-compose up -d
```

4. **Проверка статуса**
```bash
docker-compose ps
docker-compose logs -f
```

### Docker Compose конфигурация

```yaml
version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8771:8771"
    environment:
      - DATABASE_URL=sqlite:///./data/app.db
      - SECRET_KEY=${SECRET_KEY}
    volumes:
      - ./data:/app/data
      - ./config.json:/app/config.json
      - ./IP_list.json:/app/IP_list.json
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "5181:80"
    environment:
      - VITE_API_URL=http://localhost:8771/api
    depends_on:
      - backend
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:
```

## ⚙️ Настройка

### Настройка Telegram бота

1. **Создание бота**
   - Откройте [@BotFather](https://t.me/BotFather) в Telegram
   - Отправьте команду `/newbot`
   - Следуйте инструкциям для создания бота
   - Сохраните полученный токен

2. **Получение chat_id**
   - Отправьте сообщение боту
   - Перейдите по ссылке: `https://api.telegram.org/bot<TOKEN>/getUpdates`
   - Найдите ваш chat_id в ответе

3. **Настройка в приложении**
   - Откройте веб-интерфейс
   - Перейдите в **Telegram → Настройки**
   - Вставьте токен и chat_id
   - Нажмите **Сохранить**

### Настройка устройств

1. **Добавление устройств**
   - Перейдите в **Устройства**
   - Нажмите **Добавить устройство**
   - Заполните данные:
     - ID устройства (уникальный)
     - IP адрес
     - Описание
     - Категория (по умолчанию "Турникет")

2. **Массовое добавление**
   - Отредактируйте `IP_list.json`
   - Формат: `"ID": ["IP", "Описание", "Включено (1/0)"]`

### Настройка мероприятий

1. **Создание категории**
   - Перейдите в **Мероприятия**
   - Нажмите **Создать категорию**
   - Введите название и описание

2. **Выбор устройств**
   - Выберите категорию мероприятия
   - Отметьте нужные устройства
   - Нажмите **Сохранить выбор**

### Настройка уведомлений

1. **Telegram уведомления**
   - Настройте бота (см. выше)
   - Добавьте chat_id получателей

2. **Email уведомления** (опционально)
   - Настройте SMTP в .env файле
   - Включите в настройках

## ✅ Проверка установки

### Проверка backend

```bash
# Проверка API
curl http://127.0.0.1:8771/api/health

# Ожидаемый ответ
{"status": "ok", "timestamp": "2025-09-09T13:30:00Z"}
```

### Проверка frontend

```bash
# Откройте браузер
http://localhost:5181

# Проверьте консоль браузера (F12)
# Не должно быть ошибок
```

### Проверка Telegram бота

```bash
# Проверка токена
curl -X GET "https://api.telegram.org/bot<TOKEN>/getMe"

# Ожидаемый ответ
{"ok": true, "result": {"id": 123456789, "is_bot": true, "first_name": "Your Bot"}}
```

### Проверка базы данных

```bash
# Проверка файла базы данных
ls -la shaplych_monitoring.db

# Проверка таблиц (если установлен sqlite3)
sqlite3 shaplych_monitoring.db ".tables"
```

## 🔧 Устранение проблем

### Проблема: Backend не запускается

**Симптомы:**
- Ошибка "Port already in use"
- Ошибка "Module not found"

**Решения:**
```bash
# Проверка портов
netstat -tlnp | grep 8771

# Остановка процесса
sudo kill -9 <PID>

# Проверка зависимостей
pip list | grep fastapi

# Переустановка зависимостей
pip install -r requirements.txt --force-reinstall
```

### Проблема: Frontend не подключается к API

**Симптомы:**
- Ошибка CORS в консоли браузера
- "Network Error" при запросах

**Решения:**
```bash
# Проверка CORS настроек
curl -H "Origin: http://localhost:5181" \
     -H "Access-Control-Request-Method: GET" \
     -X OPTIONS \
     http://127.0.0.1:8771/api/health

# Проверка переменных окружения
echo $VITE_API_URL

# Перезапуск frontend
npm run dev -- --port 5181
```

### Проблема: Telegram бот не работает

**Симптомы:**
- "Unauthorized" ошибка
- Бот не отвечает на сообщения

**Решения:**
```bash
# Проверка токена
curl -X GET "https://api.telegram.org/bot<TOKEN>/getMe"

# Проверка конфигурации
cat config.json

# Проверка логов
tail -f bot.log

# Перезапуск бота
python advanced_bot.py
```

### Проблема: База данных заблокирована

**Симптомы:**
- "Database is locked" ошибка
- Медленные запросы

**Решения:**
```bash
# Проверка процессов
ps aux | grep sqlite

# Проверка блокировок
lsof /path/to/database.db

# Перезапуск приложения
docker-compose restart backend
```

### Проблема: Медленная работа

**Симптомы:**
- Долгая загрузка страниц
- Медленные API запросы

**Решения:**
```bash
# Проверка ресурсов
htop
df -h

# Очистка кэша
npm run build
docker system prune

# Оптимизация базы данных
sqlite3 database.db "VACUUM;"
```

## 📞 Поддержка

### Получение помощи

1. **GitHub Issues**
   - Создайте issue с описанием проблемы
   - Приложите логи и скриншоты
   - Укажите версию системы и браузера

2. **Документация**
   - Изучите [README.md](README.md)
   - Проверьте [EXAMPLES.md](EXAMPLES.md)
   - Посмотрите [TECH_SPECS.md](TECH_SPECS.md)

3. **Сообщество**
   - Discord сервер
   - GitHub Discussions
   - Stack Overflow с тегом `shaplych`

### Отчет о багах

При создании issue укажите:
- Версию системы (Windows/Linux/macOS)
- Версию Python и Node.js
- Шаги для воспроизведения
- Ожидаемое и фактическое поведение
- Логи ошибок

### Предложения по улучшению

Мы приветствуем предложения по улучшению:
- Новые функции
- Улучшения UI/UX
- Оптимизация производительности
- Документация

---

**Версия документа**: 1.0.0  
**Последнее обновление**: 2025-09-09  
**Автор**: Shaplych Development Team
