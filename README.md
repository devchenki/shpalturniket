# Shaplych Monitoring System

Современная система мониторинга турникетов с веб-интерфейсом, Telegram ботом и управлением мероприятиями.

## 📋 Содержание

- [Обзор проекта](#обзор-проекта)
- [Технологический стек](#технологический-стек)
- [Архитектура](#архитектура)
- [Структура проекта](#структура-проекта)
- [Установка и запуск](#установка-и-запуск)
- [Функционал](#функционал)
- [API документация](#api-документация)
- [Планы развития](#планы-развития)
- [Рекомендации по разработке](#рекомендации-по-разработке)

## 🎯 Обзор проекта

**Shaplych Monitoring System** - это комплексное решение для мониторинга состояния турникетов с возможностью:

- 🔍 Мониторинг устройств в реальном времени
- 📱 Telegram бот для уведомлений
- 🎪 Управление мероприятиями с выборочным включением устройств
- 📊 Аналитика и статистика
- 🎨 Современный веб-интерфейс
- ⚙️ Гибкие настройки и темы

### Основные возможности

- **Мониторинг устройств**: Ping проверки, статус онлайн/офлайн
- **Telegram интеграция**: Уведомления, управление ботом через веб-интерфейс
- **Мероприятия**: Создание категорий с выборочным включением турникетов
- **Аналитика**: Статистика доступности, время отклика
- **Настройки**: Темы, конфигурация, управление устройствами

## 🛠 Технологический стек

### Backend
- **FastAPI** - современный веб-фреймворк для Python
- **SQLModel** - ORM на основе Pydantic и SQLAlchemy
- **SQLite** - легковесная база данных
- **Uvicorn** - ASGI сервер
- **aiogram v3** - Telegram Bot API
- **icmplib** - библиотека для ping проверок

### Frontend
- **Vue 3** - прогрессивный JavaScript фреймворк
- **Vuetify 3** - Material Design компоненты
- **Pinia** - управление состоянием
- **Vue Router** - маршрутизация
- **TypeScript** - типизированный JavaScript
- **Vite** - быстрый сборщик

### Инфраструктура
- **Python 3.11+** - основной язык backend
- **Node.js 18+** - среда выполнения frontend
- **PowerShell** - скрипты для Windows
- **Git** - система контроля версий

## 🏗 Архитектура

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Frontend      │    │    Backend      │    │   Telegram      │
│   (Vue 3)       │◄──►│   (FastAPI)     │◄──►│     Bot         │
│   Port: 5181    │    │   Port: 8771    │    │   (aiogram)     │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │
         │                       │                       │
         ▼                       ▼                       ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Vuetify UI    │    │   SQLite DB     │    │   Notifications │
│   Components    │    │   Models        │    │   & Commands    │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

### Компоненты системы

1. **Web Frontend** - пользовательский интерфейс
2. **API Backend** - бизнес-логика и данные
3. **Telegram Bot** - уведомления и команды
4. **Database** - хранение конфигурации и данных
5. **Ping Service** - мониторинг устройств

## 📁 Структура проекта

```
shaplych-monitoring/
├── backend/                    # Backend приложение
│   ├── app/
│   │   ├── core/              # Основные компоненты
│   │   │   ├── config.py      # Конфигурация
│   │   │   └── db.py          # База данных
│   │   ├── models/            # Модели данных
│   │   │   ├── device.py      # Модель устройства
│   │   │   ├── event.py       # Модели мероприятий
│   │   │   └── theme.py       # Модель тем
│   │   ├── routers/           # API маршруты
│   │   │   ├── devices.py     # Управление устройствами
│   │   │   ├── ping.py        # Ping операции
│   │   │   ├── bot.py         # Telegram бот
│   │   │   ├── events_api.py  # API мероприятий
│   │   │   └── config.py      # Конфигурация
│   │   └── main.py            # Точка входа
│   └── requirements.txt       # Python зависимости
├── frontend/                   # Frontend приложение
│   ├── src/
│   │   ├── api/               # API клиент
│   │   │   └── pingApi.ts     # HTTP клиент
│   │   ├── components/        # Vue компоненты
│   │   │   └── notifications/ # Система уведомлений
│   │   ├── views/             # Страницы
│   │   │   └── ping/          # Страницы мониторинга
│   │   │       ├── dashboard/ # Панель управления
│   │   │       ├── devices/   # Устройства
│   │   │       ├── events/    # Мероприятия
│   │   │       ├── telegram/  # Telegram управление
│   │   │       └── settings/  # Настройки
│   │   ├── stores/            # Pinia stores
│   │   │   └── pingStore.ts   # Основной store
│   │   └── composables/       # Vue composables
│   │       └── useNotifications.ts
│   ├── package.json           # Node.js зависимости
│   └── vite.config.ts         # Конфигурация Vite
├── advanced_bot.py            # Telegram бот
├── Ping.py                    # Модуль ping проверок
├── Read_config.py             # Чтение конфигурации
├── IP_list.json               # Список устройств
├── config.json                # Конфигурация бота
├── bot.log                    # Логи бота
├── start.bat                  # Скрипт запуска (Windows)
├── start.ps1                  # PowerShell скрипт
└── README.md                  # Документация
```

## 🚀 Установка и запуск

### Предварительные требования

- Python 3.11+
- Node.js 18+
- Git

### Быстрый старт

1. **Клонирование репозитория**
```bash
git clone <repository-url>
cd shaplych-monitoring
```

2. **Установка зависимостей**

Backend:
```bash
cd backend
pip install -r requirements.txt
```

Frontend:
```bash
cd frontend
npm install
```

3. **Запуск системы**

**Вариант 1: Автоматический запуск (Windows)**
```bash
# Запуск через batch файл
start.bat

# Или через PowerShell
.\start.ps1
```

**Вариант 2: Ручной запуск**

Terminal 1 - Backend:
```bash
cd backend
python -m uvicorn app.main:app --host 127.0.0.1 --port 8771 --reload
```

Terminal 2 - Frontend:
```bash
cd frontend
npm run dev -- --port 5181
```

Terminal 3 - Telegram Bot (опционально):
```bash
python advanced_bot.py
```

4. **Доступ к приложению**

- **Frontend**: http://localhost:5181
- **Backend API**: http://127.0.0.1:8771
- **API Docs**: http://127.0.0.1:8771/api/docs

### Конфигурация

1. **Настройка Telegram бота**
   - Создайте бота через @BotFather
   - Обновите токен в `config.json`
   - Добавьте chat_id пользователей

2. **Настройка устройств**
   - Отредактируйте `IP_list.json`
   - Добавьте IP адреса турникетов
   - Настройте описания устройств

## ⚡ Функционал

### 📊 Панель управления (Dashboard)
- Статистика устройств (всего/онлайн/офлайн)
- Процент доступности
- Среднее время отклика
- Быстрые действия (ping всех, добавить устройство)
- Статус Telegram бота
- Последние события

### 🔧 Устройства
- Список всех устройств с статусом
- Добавление/редактирование/удаление
- Ping отдельных устройств
- Фильтрация по категориям
- Поиск по IP/описанию

### 📱 Telegram
- **Управление ботом**: Запуск/остановка/перезапуск
- **Настройки**: Токен, chat_id, параметры
- **Логи**: Просмотр логов бота в реальном времени
- **Статус**: Информация о работе бота

### 🎪 Мероприятия
- **Категории мероприятий**: Создание, редактирование, удаление
- **Выбор устройств**: Чекбоксы для выборочного включения турникетов
- **Статистика**: Количество включенных устройств по категориям
- **Управление**: Гибкая настройка под конкретные события

### 📈 Аналитика
- Графики доступности устройств
- Статистика времени отклика
- История событий
- Экспорт данных

### ⚙️ Настройки
- **Темы**: Светлая/темная, цветовые схемы
- **Уведомления**: Настройка алертов
- **Конфигурация**: Параметры системы
- **Экспорт/Импорт**: Резервное копирование

## 📚 API документация

### Основные endpoints

#### Устройства
- `GET /api/devices` - Список устройств
- `POST /api/devices` - Создать устройство
- `PUT /api/devices/{id}` - Обновить устройство
- `DELETE /api/devices/{id}` - Удалить устройство

#### Ping операции
- `POST /api/ping/all` - Ping всех устройств
- `POST /api/ping/{device_id}` - Ping конкретного устройства

#### Telegram бот
- `GET /api/bot/status` - Статус бота
- `POST /api/bot/start` - Запустить бота
- `POST /api/bot/stop` - Остановить бота
- `POST /api/bot/restart` - Перезапустить бота
- `GET /api/bot/logs` - Логи бота

#### Мероприятия
- `GET /api/events/categories` - Список категорий
- `POST /api/events/categories` - Создать категорию
- `PUT /api/events/categories/{id}` - Обновить категорию
- `DELETE /api/events/categories/{id}` - Удалить категорию
- `POST /api/events/categories/{id}/devices` - Добавить устройства

#### Конфигурация
- `GET /api/config` - Полная конфигурация
- `GET /api/config/devices` - Конфигурация устройств
- `GET /api/config/bot` - Конфигурация бота
- `PUT /api/config/bot` - Обновить конфигурацию бота

### Server-Sent Events
- `GET /api/events/stream` - Поток событий в реальном времени

## 🎯 Планы развития

### Краткосрочные цели (1-2 месяца)

#### 🔧 Техническая оптимизация
- [ ] **Docker контейнеризация**
  - Dockerfile для backend
  - Docker Compose для полной системы
  - Автоматическое развертывание

- [ ] **Мониторинг и логирование**
  - Структурированные логи (JSON)
  - Метрики Prometheus
  - Health checks
  - Алерты при критических ошибках

- [ ] **Безопасность**
  - JWT аутентификация
  - HTTPS поддержка
  - Валидация входных данных
  - Rate limiting

#### 📱 Функциональные улучшения
- [ ] **Расширенная аналитика**
  - Графики трендов доступности
  - Экспорт отчетов (PDF, Excel)
  - Настраиваемые дашборды
  - Сравнение периодов

- [ ] **Улучшенные уведомления**
  - Email уведомления
  - Push уведомления в браузере
  - Настраиваемые шаблоны сообщений
  - Эскалация алертов

- [ ] **Автоматизация**
  - Автоматический перезапуск устройств
  - Планировщик задач (cron)
  - Автоматические отчеты
  - Интеграция с системами мониторинга

### Среднесрочные цели (3-6 месяцев)

#### 🏢 Корпоративные функции
- [ ] **Многопользовательская система**
  - Роли и права доступа
  - Аудит действий пользователей
  - Персональные настройки
  - Командная работа

- [ ] **Интеграции**
  - Active Directory/LDAP
  - Slack/Teams уведомления
  - Webhook API
  - REST API для внешних систем

- [ ] **Масштабируемость**
  - Кластеризация backend
  - Load balancing
  - Горизонтальное масштабирование
  - Кэширование (Redis)

#### 📊 Расширенная аналитика
- [ ] **BI и отчетность**
  - Интерактивные дашборды
  - Машинное обучение для прогнозирования
  - Аномалии и тренды
  - Бизнес-метрики

- [ ] **Мобильное приложение**
  - React Native приложение
  - Push уведомления
  - Офлайн режим
  - Геолокация

### Долгосрочные цели (6+ месяцев)

#### 🌐 Платформа как сервис
- [ ] **SaaS решение**
  - Мультитенантность
  - Облачное развертывание
  - API для разработчиков
  - Marketplace плагинов

- [ ] **ИИ и автоматизация**
  - Предиктивная аналитика
  - Автоматическое обнаружение проблем
  - Рекомендации по оптимизации
  - Чат-бот с ИИ

- [ ] **Глобальная экосистема**
  - Партнерская программа
  - Сертификация интеграторов
  - Образовательные материалы
  - Сообщество разработчиков

## 💡 Рекомендации по разработке

### 🏗 Архитектурные принципы

#### Backend
- **Clean Architecture**: Разделение на слои (API, Business, Data)
- **SOLID принципы**: Единая ответственность, открытость/закрытость
- **Dependency Injection**: Слабая связанность компонентов
- **Event-Driven**: Асинхронная обработка событий

#### Frontend
- **Component-Based**: Переиспользуемые Vue компоненты
- **Composition API**: Современный подход Vue 3
- **TypeScript**: Строгая типизация
- **Reactive State**: Централизованное управление состоянием

### 🔧 Инструменты разработки

#### Обязательные
- **IDE**: VS Code с расширениями Vue, Python
- **Linting**: ESLint, Prettier, Black, isort
- **Testing**: Jest, Vitest, pytest
- **Git Hooks**: pre-commit, pre-push

#### Рекомендуемые
- **Monitoring**: Sentry для отслеживания ошибок
- **Performance**: Lighthouse, Web Vitals
- **Documentation**: Swagger/OpenAPI, Storybook
- **CI/CD**: GitHub Actions, Docker Registry

### 📝 Стандарты кодирования

#### Python (Backend)
```python
# Используйте type hints
def ping_device(device_id: str, timeout: int = 5) -> PingResult:
    """Ping устройство и вернуть результат."""
    pass

# Async/await для I/O операций
async def get_device_status(device_id: str) -> DeviceStatus:
    pass

# Pydantic модели для валидации
class DeviceCreate(BaseModel):
    device_id: str
    ip: str
    description: str
```

#### TypeScript (Frontend)
```typescript
// Строгая типизация
interface Device {
  id: number
  device_id: string
  ip: string
  status: 'online' | 'offline' | 'warning'
}

// Composables для логики
export function useDevices() {
  const devices = ref<Device[]>([])
  
  const loadDevices = async (): Promise<void> => {
    // логика загрузки
  }
  
  return { devices, loadDevices }
}
```

### 🧪 Тестирование

#### Backend тесты
```python
# Unit тесты
def test_ping_device_success():
    result = ping_device("192.168.1.1")
    assert result.status == "online"

# Integration тесты
async def test_device_api():
    response = await client.post("/api/devices", json=device_data)
    assert response.status_code == 201
```

#### Frontend тесты
```typescript
// Component тесты
describe('DeviceCard', () => {
  it('displays device information', () => {
    const device = { id: 1, device_id: 'T001', ip: '192.168.1.1' }
    render(DeviceCard, { props: { device } })
    expect(screen.getByText('T001')).toBeInTheDocument()
  })
})

// Store тесты
describe('pingStore', () => {
  it('loads devices successfully', async () => {
    const store = usePingStore()
    await store.loadDevices()
    expect(store.devices).toHaveLength(3)
  })
})
```

### 📊 Мониторинг и отладка

#### Логирование
```python
# Структурированные логи
import structlog

logger = structlog.get_logger()

logger.info(
    "Device ping completed",
    device_id=device_id,
    response_time=response_time,
    status=status
)
```

#### Метрики
```python
# Prometheus метрики
from prometheus_client import Counter, Histogram

ping_requests = Counter('ping_requests_total', 'Total ping requests')
ping_duration = Histogram('ping_duration_seconds', 'Ping duration')
```

### 🔒 Безопасность

#### Backend
- Валидация всех входных данных
- SQL injection защита (SQLModel)
- CORS настройки
- Rate limiting
- HTTPS только

#### Frontend
- XSS защита (Vue автоматически)
- CSRF токены
- Безопасное хранение токенов
- Content Security Policy

### 🚀 Производительность

#### Backend оптимизация
- Асинхронные операции
- Кэширование запросов
- Пагинация больших списков
- Connection pooling

#### Frontend оптимизация
- Lazy loading компонентов
- Виртуализация списков
- Debounce для поиска
- Service Worker для кэширования

## 🤝 Участие в разработке

### Процесс разработки

1. **Fork репозитория**
2. **Создание feature branch**: `git checkout -b feature/new-feature`
3. **Разработка с тестами**
4. **Commit с описанием**: `git commit -m "feat: add device filtering"`
5. **Push в fork**: `git push origin feature/new-feature`
6. **Создание Pull Request**

### Соглашения

- **Commits**: Conventional Commits (feat:, fix:, docs:, etc.)
- **Branches**: feature/, bugfix/, hotfix/, release/
- **Code Review**: Обязательно для всех PR
- **Tests**: Покрытие тестами новых функций

### Контакты

- **Issues**: GitHub Issues для багов и предложений
- **Discussions**: GitHub Discussions для вопросов
- **Email**: [your-email@domain.com]

---

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 🙏 Благодарности

- Vue.js команда за отличный фреймворк
- FastAPI за быстрый и современный API
- Vuetify за красивые компоненты
- Всем контрибьюторам проекта

---

**Shaplych Monitoring System** - современное решение для мониторинга инфраструктуры с открытым исходным кодом.